#include<stdio.h>
struct_fraction
{
  int num,den;
};
typedef struct fraction fraction;
fraction input_fraction;
{
  fraction f;
  printf("enter fraction");
  scanf("%d %d",&f.num,&f.den);
  return f;
}
fraction largest_fraction(fraction f1, fraction f2,fraction f3)
{
  int sden=f1.den*f2.den*f3
}
int a,b,c;
c=f3.numerator